import {
  backgroundWhite,
  contentWhite,
  backgroundTextWhite,
  backgroundDark,
  backgroundTextDark,
  contentDark,
} from "./variables";

export const ThemeWhite = {
  body: backgroundWhite,
  inside: contentWhite,
  text: backgroundTextWhite,
  filter: "",
};

export const ThemeDark = {
  body: backgroundDark,
  inside: contentDark,
  text: backgroundTextDark,
  filter: "invert(100%)",
};
